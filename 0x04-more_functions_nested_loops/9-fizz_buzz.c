#include <main.h>
/**
* c program
* Return: 0
*/

int main(void)
{
int n;

 for (n =1 ; n <= 100; n++)
 {
     if (n % 3 === && !n % 5 == 0)
      printf("fizz");
      else if (n % 3 === && n % 5 == 0)
      printf("fizzBuzz");
      else if (!n % 3 === && n % 5 == 0)
      printf("Buzz");
      else 
        printf("%d" , n);
        
        if(num != 100)
        printf(" ");
        else 
          printf("\n");
 }
 return(0);
}
